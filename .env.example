NEXT_PUBLIC_SITE_URL=https://petit.example.com

# Resend
# Create a full-access API key at https://resend.com/api-keys
# (a "sending-only" key returns 401 restricted_api_key on the contacts endpoint).
# Segment IDs are configured per email form via frontmatter (cta.segmentId).
RESEND_API_KEY=
